package com.projectcod.test;

import org.junit.BeforeClass;
import org.junit.Test;

import com.projectcod.repositories.CustomerRepository;
import com.projectcod.service.CustomerService;

public class CustomerServiceJUnitTest {
	/*
	 * private static CustomerService customerService; private static
	 * CustomerRepository customerRepository;
	 * 
	 * @BeforeClass public static void setup() { customerRepository =
	 * Mockito.mock(CustomerRepository.class); customerService = new
	 * CustomerService(customerRepository); }
	 * 
	 * @Test public void testFindByEmail() {
	 * 
	 * }
	 */
}
